---
description: "Input validation and attack-surface defenses. Apply for API, forms, schemas, uploads."
globs:
  - "**/api/**/*"
  - "**/*schema*.*"
  - "**/validation/**/*"
  - "**/*upload*.*"
  - "**/actions/**/*"
alwaysApply: false
---

# Input Validation

- Trust nothing external; server-side validation is mandatory; client checks are UX only.
- Prefer allowlists; encode/escape for output context.
- Mitigate SQLi (parameterized/ORM), XSS, CSRF, command/path injection, SSRF (allowlist hosts).
- Uploads: size/type/magic-byte checks; non-executable storage; server-generated filenames.
- Schema-validate at boundaries; authorize per resource action (IDOR); rate-limit abuse.
- Fail closed; user-safe messages vs detailed logs.

Korean rationale: `docs/pvc-guide/ko/security/input-validation.md`
