---
description: "프로젝트별 기술·업무 맥락 고정 스펙. 복사 후 이 파일을 반드시 채운다. 스택은 예시일 뿐 강제하지 않는다."
alwaysApply: true
---

# 프로젝트 맥락 (고정 스펙 템플릿)

> 이 파일은 프로젝트마다 다르다. 아래 항목을 **실제 값으로 교체**한다.  
> 미기입 항목을 AI가 추측해 채우지 못하게 하는 것이 목적이다.  
> 아래 Tech Stack 예시는 **형식 예시**일 뿐, 특정 프레임워크를 강제하지 않는다.

## 1. 프로젝트 목적

- 제품/서비스 목적:
- 주요 사용자·사용 환경:
- 핵심 성공 기준:
- 명시적으로 하지 않을 것 (Non-goals):

## 2. Tech Stack

형식 예 — 값을 프로젝트에 맞게 바꾼다.

```text
- Runtime:
- Language / Type system:
- App framework (web/mobile/desktop/api):
- UI / Styling:
- Data store / ORM:
- Auth approach:
- Hosting / Deploy:
- CI:
- Observability:
- Major external integrations:
```

## 3. Forbidden (프로젝트 금지 사항)

명시하지 않으면 AI가 임의 스타일을 도입할 수 있다. 예:

```text
- (예) React class components 금지
- (예) 검증 없는 any 남발 금지
- (예) 인라인 스타일 남발 금지 (동적 값 예외)
- (예) 운영 DB를 로컬 실험에 사용 금지
```

- Forbidden:
-

## 4. Performance Budget

웹이면 Core Web Vitals Good을 기본으로 하고 필요 시 강화한다.

```text
- LCP: ≤ 2.5s (p75)
- INP: ≤ 200ms (p75)
- CLS: ≤ 0.1 (p75)
- Initial JS (gzip): (프로젝트 기입)
```

- Performance Budget:
-

## 5. Code Language & Naming

```text
- 주석/문서:
- 식별자(변수·함수·타입):
- 커밋 메시지: Conventional Commits (언어: )
- 브랜치 전략: GitHub Flow | 기타(명시)
```

## 6. 도메인·업무 규칙

- 핵심 도메인 용어:
- 반드시 지킬 업무·법적·보안 제약:
- 승인 없이 변경 금지 정책:

## 7. 아키텍처 메모

- 주요 진입점:
- 모듈 경계·의존성 방향:
- 데이터 흐름 요약:
- API envelope / 에러 형식:
- 알려진 기술 부채·제한:

## 8. 환경·시크릿

- 환경 이름 (dev/staging/prod):
- 시크릿 저장 위치:
- `.env.example` 위치:

## 9. 참고 문서

- `CODEMAP.md`:
- `ARCHITECTURE.md`:
- `DESIGN.md` / `TYPEGUIDE.md`:
- OpenAPI / 스키마:
- 기타:
