---
description: "Project facts card. Fill before coding. Always on. Keep short—facts only, not a long questionnaire."
alwaysApply: true
---

# Project Facts

> Keep this card short. Optionally summarize answers from `docs/pvc/개발방향-설문.md` (recommended planning survey—not required to start). Empty fields make the agent guess.
> Replace the example values below for each real project.

## Product

- Product / service goal: _(fill)_
- Primary users & environments: _(fill)_
- Success criteria: _(fill)_
- Non-goals: _(fill)_

## Stack (example — replace if different)

```text
- Runtime: Node 22
- Language: TypeScript (strict)
- Framework: Next.js 15 (App Router) / React 19
- Styling: Tailwind CSS v4
- Data: PostgreSQL + Prisma
- Auth: _(fill)_
- Hosting: _(fill)_
- Package manager: _(pnpm | npm | bun)_
- App entry: _(e.g. src/app)_
- API entry: _(e.g. src/app/api)_
- Tests: _(e.g. Vitest, Playwright)_
```

## Forbidden

```text
- Unapproved production DB use
- Secrets in client bundles or git
- Unapproved new major dependencies
- (Add project-specific bans here)
```

## Required checks

```text
- _(e.g. pnpm lint && pnpm test && pnpm build)_
```

## AI defaults (adjust per stack)

```text
- Prefer server components / server data fetching when the stack supports it
- Style via design tokens / shared utilities, not one-off page CSS
- New folders only when a domain has multiple related files
```

## Performance budget (defaults)

```text
- LCP < 2.5s (p75) | INP ≤ 200ms | CLS ≤ 0.1
```

Korean template commentary (human only): `docs/pvc-guide/ko/project/project-context.md`
