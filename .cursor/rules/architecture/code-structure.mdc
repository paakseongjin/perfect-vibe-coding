---
description: "코드 구조·모듈·폴더·명명 규칙과 웹 표준 기반 구현 기준."
alwaysApply: true
---

# 코드 구조 및 웹 환경 구축 기준

## 1. 모듈·폴더·관심사 분리

- UI 표현, 상태 관리, 비즈니스 규칙, 데이터 접근, 외부 API 연동, 유틸리티, 설정, 테스트를 명확히 분리한다.
- 하나의 파일이나 함수가 여러 책임을 과도하게 맡지 않도록 한다.
- 공통 로직은 검증 후 재사용 가능한 모듈, 컴포넌트, 유틸리티 또는 서비스로 분리한다.
- 페이지별 구현에 공통 스타일, 공통 데이터 로직, 공통 비즈니스 규칙을 중복 작성하지 않는다.
- 기존 프로젝트의 파일 구조, 명명 규칙, 모듈 경계, 의존성 방향을 우선 존중한다.
- 구조 변경이 필요한 경우에는 기존 구조의 문제점, 변경 범위, 영향도, 마이그레이션 방법을 먼저 보고하고 승인을 받는다.

## 2. 표준 기반 구현

- 국제 웹 표준, 브라우저 호환성, 시맨틱 HTML, CSS 표준, 표준 JavaScript API를 우선 사용한다.
- 브라우저 또는 프레임워크에 과도하게 종속되는 구현은 필요한 근거가 있을 때만 도입한다.
- 레이아웃은 콘텐츠 구조와 반응형 요구사항에 따라 CSS Grid와 Flexbox를 적절히 사용한다.
- 널리 쓰이는 그리드와 레이아웃 관행을 참고하되, 기계적으로 고정된 숫자나 프레임워크 규칙을 적용하지 않는다.
- 화면 크기, 운영 기기, 사용자 업무 흐름을 기준으로 콘텐츠 폭, 열 수, 간격, 터치 영역을 설계한다.

## 3. 성능 기준

- 초기 화면에 필요한 코드와 자산만 우선 로드한다.
- 이미지, 폰트, 스크립트, 스타일시트, 번들 크기를 불필요하게 키우지 않는다.
- 대용량 이미지와 미사용 자산을 그대로 배포하지 않는다.
- 반복 렌더링, 불필요한 API 호출, 중복 이벤트 바인딩, 메모리 누수 가능성을 점검한다.
- 성능 개선은 측정 가능한 근거와 실제 사용자 경험의 개선 효과를 기준으로 수행한다.
